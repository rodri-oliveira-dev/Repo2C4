model {
  el_library_repo = softwareSystem "Library-only repository" {
    #requires-review
    metadata {
      architectureId "el_library_repo"
      evidenceIds ["ev_library_kind", "ev_library_project"]
      reviewStatus "requiresReview"
      reviewReason "No executable or host candidate exists; the focal system boundary is unresolved and no C2 container is proposed."
    }
  }
}
